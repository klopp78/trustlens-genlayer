# Context note

Independent context for TrustLens.

GenLayer consensus is useful here because the application asks validators to fetch the same sources, commit rendered snapshots, and return a durable verdict instead of letting a frontend keyword score decide whether an account or post is trustworthy.
